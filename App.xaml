<Application x:Class="HRS.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:HRS.Converters"
             StartupUri="Views/LoginView.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Blue" SecondaryColor="Blue" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                
                <!-- LUMINOUS Theme Resources (Light Mode) -->
                <ResourceDictionary>
                    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
                    <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
                    <local:InverseBooleanConverter x:Key="InverseBooleanConverter" />
                    
                    <!-- Boolean to Opacity Converter (for disabled states) -->
                    <local:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter" />
                    
                    <!-- Multi-value converter for checking if all required fields are filled -->
                    <local:AllValuesFilledConverter x:Key="AllValuesFilledConverter" />
                    
                    <!-- Status to Color Brush Converter for reservation status badges -->
                    <local:StatusToColorBrushConverter x:Key="StatusToColorBrushConverter" />
                    
                    <!-- String to Visibility Converter for showing elements when string is not empty -->
                    <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
                    
                    <!-- Color Palette -->
                    <Color x:Key="AppBackground">#F9FAFB</Color>
                    <Color x:Key="SidebarBackground">#FFFFFF</Color>
                    <Color x:Key="CardBackground">#FFFFFF</Color>
                    <Color x:Key="CardHoverBackground">#F3F4F6</Color>
                    
                    <Color x:Key="LuminousAccent">#1D4ED8</Color>
                    <Color x:Key="TextPrimary">#111827</Color>
                    <Color x:Key="TextSecondary">#6B7280</Color>
                    <Color x:Key="SuccessGreen">#10B981</Color>
                    <Color x:Key="DangerRed">#EF4444</Color>
                    
                    <!-- Brushes -->
                    <SolidColorBrush x:Key="AppBackgroundBrush" Color="{StaticResource AppBackground}" />
                    <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="{StaticResource SidebarBackground}" />
                    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackground}" />
                    <SolidColorBrush x:Key="CardHoverBrush" Color="{StaticResource CardHoverBackground}" />
                    <SolidColorBrush x:Key="LuminousAccentBrush" Color="{StaticResource LuminousAccent}" />
                    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}" />
                    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}" />
                    <SolidColorBrush x:Key="SuccessGreenBrush" Color="{StaticResource SuccessGreen}" />
                    <SolidColorBrush x:Key="DangerRedBrush" Color="{StaticResource DangerRed}" />
                    <SolidColorBrush x:Key="DividerBrush" Color="#E5E7EB" />
                    <SolidColorBrush x:Key="BadgeBackgroundBrush" Color="#F3F4F6" />

                    <!-- Backward Compatibility -->
                    <SolidColorBrush x:Key="NocturnalAccentBrush" Color="{StaticResource LuminousAccent}" />
                    
                    <!-- Styles & Animations -->
                    <Style x:Key="LuminousCardStyle" TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
                        <Setter Property="CornerRadius" Value="12" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                        <Setter Property="Padding" Value="20" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource LuminousAccentBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <Style x:Key="LuminousRaisedButtonStyle" TargetType="Button">
                        <Setter Property="Background" Value="{DynamicResource LuminousAccentBrush}" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="Padding" Value="15,10" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="{Binding (materialDesign:ButtonAssist.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
                                                <Grid>
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    <Border x:Name="hoverOverlay" Background="Black" Opacity="0" IsHitTestVisible="False" CornerRadius="{Binding (materialDesign:ButtonAssist.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}" />
                                                </Grid>
                                            </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="hoverOverlay" Property="Opacity" Value="0.1" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="hoverOverlay" Property="Opacity" Value="0.2" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Opacity" Value="0.5" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style x:Key="LuminousSidebarButtonStyle" TargetType="Button">
                        <Setter Property="Height" Value="48" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="#9CA3AF" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Margin" Value="10,2" />
                        <Setter Property="Padding" Value="15,0" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderThickness="3,0,0,0" BorderBrush="Transparent">
                                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#0DFFFFFF" />
                                            <Setter TargetName="border" Property="BorderBrush" Value="#10B981" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#1AFFFFFF" />
                                            <Setter TargetName="border" Property="BorderBrush" Value="#10B981" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
